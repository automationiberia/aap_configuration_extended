---
bugfixes:
  - trigger pull request CI from workflow_run after the trusted pre-commit approval
    workflow completes, avoiding PAT-based label coordination and pull_request
    races with GITHUB_TOKEN
  - remove ci-approved labels now that approval completion is the gate signal
  - log CI approval routing and environment protection context in workflow summaries
  - fix pre-commit-tests cache key expression that made the workflow file invalid
  - fix invalid actions/cache commit pin that prevented cache restore and save
  - remove workflow_run branches filter that blocked tests after pull_request_target
    approval because the triggering run reports the PR head branch, not devel
